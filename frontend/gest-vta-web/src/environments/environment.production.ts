/** Solo build producción (`ng build` / `--configuration=production`). Ver `angular.json` → fileReplacements. */
export const environment = {
  production: true,
  apiUrl: 'http://38.242.219.173:82',
};
